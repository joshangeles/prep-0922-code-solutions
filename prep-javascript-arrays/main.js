const language = [
  'html',
  'css',
  'javascript',
  'react',
  'node',
  'postgres'
];
var firstElement = language[0];
var thirdElement = language[2];
var length = language.length;
var lastElement = language[(length - 1)];
console.log(language);
console.log('The first element of the array is...' + firstElement);
console.log('The third element of the array is...' + thirdElement);
console.log('The length of the array...' + length);
console.log('The last element of the array is...' + lastElement);
